
$(document).on("ready", gogogo);
function gogogo(){
    var scrollorama = $.scrollorama({
        blocks:'.scrollblock'
    });
    //scrollorama.animate('#Header',{ delay: 400, duration: 600, property:'top', start:0, end:0, });
    //scrollorama.animate('#Perfil',{ duration: 1000, property:'right', start:-1500,end:0});
    //scrollorama.animate('#Perfil',{ duration: 500, property:'opacity', start:0.5,end:1});

         
}
